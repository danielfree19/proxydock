# Binaries
*.exe
*.dll
*.so
*.dylib
manager-api

# Test
*.test
*.out
coverage.txt

# IDE
.vscode/
.idea/
*.swp

# OS
.DS_Store

# Local data
deploy/docker-compose/data/
deploy/docker-compose/secrets/*.local

# Go
vendor/
apps/web/node_modules/
apps/web/dist/
apps/web/tsconfig.tsbuildinfo

# Embedded SPA build artifacts (the placeholder index.html is committed,
# but built assets are not — they're regenerated by `make web-embed`).
apps/api/internal/webui/dist/assets/

# Editor / agent caches
.claude/
.aider*
.cursorignore
